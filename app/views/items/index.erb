<div class="jumbotron">
	<div class="container">

		<h2>Welcome to the Blind Auction</h2>

</div></div>


<div>
	<div class="container">
		<h3>Click on the links below to see product details</h3>
		<ul class="product-list">
			<% @items.each do|item| %>	
			<li><a href="/items/<%= item.id%>"><%= item.title %></li>
			<% end %>
		</ul>

		<!-- <p><h3>Active Items</h3></p>

		<ol>
			<% @active_items.each do|item| %>
			<li><a href="/items/<%= item.id%>"><%= item.title %></a></li>
			<% end %>
		</ol> -->
	</div>
</div>